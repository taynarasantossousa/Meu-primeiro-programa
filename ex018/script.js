var salario = Number(prompt("Digie seu salario:"))
var bonus = Number(prompt("Digite o bonus:"))

var salarioTotal = salario + bonus

alert(`O valor do seu salário é de ${salario} e o valor do bonus é ${bonus}
    
    o salário total é: ${salarioTotal}`)